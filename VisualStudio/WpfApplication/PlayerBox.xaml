<UserControl x:Class="WpfApplication.PlayerBox"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Border Name="border" Style="{StaticResource infoBlock}" >
        <StackPanel Style="{StaticResource playerInfoBlock}">
            <Label Name="playerName" Style="{StaticResource playerName}" Content="{Binding Tag, ElementName=playerName}" />
            <Label Name="playerPopulation" Style="{StaticResource playerInfo}" Content="{Binding Tag, ElementName=playerPopulation}" />
            <Label Name="playerScore" Style="{StaticResource playerInfo}" Content="{Binding Tag, ElementName=playerScore}" />
            <Label Name="playerNbUnit" Style="{StaticResource playerInfo}" Content="{Binding Tag, ElementName=playerNbUnit}" />
        </StackPanel>
    </Border>
</UserControl>
